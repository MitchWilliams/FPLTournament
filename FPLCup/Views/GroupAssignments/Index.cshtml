@model IEnumerable<FPLTournament.Models.GroupAssignment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.group.GroupId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.team.manager)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.group.GroupId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.team.manager)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupAssignmentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupAssignmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupAssignmentId })
        </td>
    </tr>
}

</table>
